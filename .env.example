# Content OS settings. Copy to .env to override. No API keys are used.
# Backend for LLM stages: claude (default, Claude in chat/Cowork) | mock (tests) | ollama (later)
CONTENTOS_BACKEND=claude
OLLAMA_HOST=http://localhost:11434